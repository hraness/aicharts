---
title: Build AI Charts Usage
description: Deliver content-minimized local usage collection, shared Hraness identity, durable ingestion and opt-in rankings.
type: plan
area: usage-leaderboard
status: in-progress
---

# AI Charts Usage implementation plan

10 September 2026 · source implementation and remaining delivery work

## Outcome

Deliver an open-source Codex/Claude Code usage collector, opt-in background sync, Hraness-authenticated private analytics and public rankings on aicharts.io. Shared profiles support GitHub, X, LinkedIn, website and R2 avatars. Explicit subscription history and consenting usage cohorts can later supply reviewed calculator datasets. A skill queries both existing benchmarks and personal measurements efficiently.

Implementation began from AI Charts `f9ac128` on `codex/usage-foundation-20260910`. The first source slice implements a local-only numeric protocol, selective historical readers, CLI dry-run and TypeScript rollups. The earlier offline probe is exploratory evidence, not the v1 protocol. Authentication, network ingestion and background installation remain disabled. Current procedure and byte contracts live in `docs/usage-local.md` and `docs/usage-wire-v1.md`.

## Baseline and constraints

- Inspected AI Charts main: `b5ee7dbf38cef04a70166baaf49a42c237dd79d1`; Suite Accounts: `832e9d2364617b54b06872a81fab7a95791f4fd4`; Tokscale reference: `d9a45a65ddfaf21bea2fe9de692aca89f46e67bf`.
- Local `/Users/bg/Documents/aicharts` was older than production. Execute in a task-owned current worktree after checking current branch/base/status and all applicable `AGENTS.md`; do not build against or overwrite the stale checkout.
- Preserve the current static-data benchmark routes, refresh guards, public data contracts, calculator source history, privacy-constrained analytics, accessibility and canonical domain.
- Keep shared identity in the Accounts authority. AI Charts owns usage, publishing consent, provider-account attribution and ranking semantics. Publish immutable reviewed shared contracts before consuming them. No sibling-path dependencies or assumed coordinated `main` deployment.
- Root/integrator owns manifest/lockfile changes, protocol/registry convergence, public route contracts, root check scripts, rollout and final gates. Freeze these interfaces before parallel workers edit callers.
- Check and refresh the managed repository baseline within the same task-owned change, preserving unmanaged rules. Respect the currently installed host scheduler and repository guidance rather than substituting or bypassing either.
- Broad/final portable work: installed absolute host scheduler, `heavy` compute lane. Authenticated browser/dev-server flows: one `browser-auth` owner. Signing/Keychain/native package proof: `mac-native`. Focused pure tests normally need no host lease. Never hold a compute lease while waiting for CI or a deployment.
- AI Charts requires `bun run check`; its current command includes generated-data checks, typecheck, lint, tests, build and browser checks. Add new Rust/worker validation to the reviewed aggregate contract, never silently omit it. Suite Accounts requires its own current `bun run check` and documented package/browser/release evidence. Resolve the Accounts authority's current owning repository and exact gates before editing it; the SDK is not the authority service.
- Protected-main task-owned PRs, independent review, exact-head required CI, merge and production verification are mandatory. Continue routine authorized delivery without asking again, but do not bypass auth, branch protection, provider approval or unknown paid-resource scope.
- Before provisioning, inspect exact provider account/resource/region/plan and existing owner-controlled R2 capacity; check the documented Marketplace route first, then supported provider APIs/CLI if appropriate. No speculative paid resources or durable preview backends.

## Phase map

### Continuous completion target

The user requested continuous execution through a usable, polished end-to-end release on 11 September 2026. Intermediate source merges are checkpoints, not stopping points. Continue implementation, independent review, relevant validation, protected delivery and live qualification until the requested product works or a material decision, missing authority or unavoidable interactive authentication blocks further safe progress. Do not claim comparative superiority, formal correctness, cross-platform support or absence of covert channels beyond the evidence actually established.

Current orchestration step: integrate and qualify dormant account admission after delivering native anchor source, the Accounts profile editor, source-free inspection and the portable benchmark/private-usage skill. Complete-prefix CLI collection, native credential types, custody engine, shared admission bytes, immutable R2 helpers, native sender, reindex and retained-genesis recovery are delivered. Native transport, broader metric collection and product UI remain further integration work. The integrator owns Worker implementation, convergence files and delivery. Bounded workers own standalone native modules, tests and independent review. No private sessions or credentials are needed for these source steps. The managed baseline tool still refuses the pre-existing mixed legacy/current markers; preserve that guard and continue unaffected work.

Frozen next admission contract: the account-owned Durable Object orders acceptance and revocation. Uploads carry the enrolled device proof, durable per-device sequence and canonical payload hash, never the polling secret or namespace key. Reserve, stage and validate before a final revocation check freezes a receipt-pending decision; make its immutable receipt durable before accepted visibility or acknowledgment. Explicit corrections replace/tombstone occurrence keys with expected predecessors and can decrease totals. Whole-day replacement from one device is prohibited because other devices and unavailable historical sources may own additional records. A contiguous immutable account journal must preserve acceptance, correction, revocation and deletion through externally fenced restore.

The launch audit also requires complete-prefix source checkpoints before automatic sync, native vault custody and separately tested reader/uploader capabilities. Existing imported frames lack model/account/auth/prompt/interval evidence; preserve unknown values until an additive end-to-end measurement contract is qualified. The initial account index allows 100,000 retained subjects but caps live heads at 65,536 per UTC day, matching the current rollup ceiling without silently raising it. Existing calculator pricing is not a Vercel AI Gateway usage catalog, and current plan observations must not be applied to unidentified historical usage.

Paths below are proposed new module scopes, not claims that those modules exist today. Keep Rust and worker code product-owned in the AI Charts repository initially; a separate shared package requires demonstrated consumers.

| Phase | Deliverable | Depends on | Owned write scope | Parallel opportunity |
| --- | --- | --- | --- | --- |
| 0 | Accepted contracts and fixtures | None | Integrator: `crates/aicharts-protocol`, `lib/usage/contracts`, `kb/plans/usage-leaderboard.md` | None until frozen |
| 1 | Hraness web/device identity and shared media profile | 0 | Accounts-authority owner; Suite Accounts `src/identity`, profile/auth packages | 2 after shared schema freeze |
| 2 | Offline real-provider collector and numeric ledger | 0 | Collector owner: `crates/aicharts-core`, `crates/aicharts-cli`, `crates/aicharts-ledger` | 1 |
| 3 | Authenticated durable ingestion/query backend | 0, 1 | Backend owner: `services/usage-worker`, `lib/usage/server` | 2 for non-shared integration work |
| 4 | Private dashboard and public leaderboard | 1, 2, 3 | UI owner: usage/profile/ranking routes and `components/usage` | 5 and 6 after API freeze |
| 5 | Benchmark/personal usage skill | 2, 3 | Skill owner: `skills/aicharts`; CLI query module assigned exclusively | 4 and 6; no shared CLI file edits |
| 6 | Background service and optional tray | 1, 2, 3 | Native owner: platform service modules, tray crate, packaging | 4 and 5 after CLI command contract freeze |
| 7 | Trust review, groups/sharing and calculator cohort | 4, 5, 6 | Assigned trust, sharing, study modules; integrator owns rollout | Sequential integration with disjoint focused lanes |

No phase may edit a convergence file simultaneously. Workers submit required manifest changes to the integrator rather than racing lockfiles. Every phase ends in independent review and its relevant gate; the integration owner runs final aggregate validation after convergence.

## Phase 0: Production contracts

The 11 September addition requests daily average turn length in elapsed runtime, tokens, and tool calls. Adopt completed root turns, direct-only token/tool attribution, whole-turn terminal UTC-day assignment, independent exact sum/eligible-count pairs, and explicit unknown/partial coverage. Aborted turns stay separate; EOF, role labels and session activity cannot establish completion or human authorship. A separate local terminal-turn projection and pure daily rollup are implemented in `lib/usage/turns.ts` and integrated into daily rollup output, with no AICU/admission/collector changes. The 22 focused turn/rollup tests pass, including weighted means, zero versus unknown, ambiguous cohorts, root/child separation, duplicate refusal, corrected-head recomputation, midnight, bounds and exact sums above 2^53. [PR 186](https://github.com/hraness/aicharts/pull/186) delivered the independently reviewed projection at `ea7468dc06be3b044fcf9abbccb13d55950a356f`, after the full local/KB gates and exact-head/merge CI passed. Production `dpl_CiET6WPfFHWmHKFZyE6EswNyMyys` matched that merge; nine anonymous probes preserved benchmark data and disabled auth, with no errors in the bounded log window. Real metrics still await qualified lifecycle, turn membership, and dispatch evidence. Current contract: `docs/usage-turns.md`.

- **Status:** Partial
- **Depends on:** none
- **Objective:** versioned measurement, identity, privacy and query contracts with representative fixtures.
- **Scope:** strict numeric occurrence/prompt/interval/account record types, registry/version strategy, time/price dimensions, device sequences/revisions, capability claims, retention/deletion contract and reference fixtures. Adopt or replace the offline v0 probe deliberately; it is not the v1 wire by default.
- **Out of scope:** real session scanning, auth enrollment, public uploads, production resources.
- **Acceptance:** no arbitrary content fields; unknown stays unknown; byte/record bounds enforced before allocation; exact token-subset rules; 15m activity and independent 16m concurrency definitions; idempotency/correction state machine and privacy threat model reviewed. Real model aliases and pricing tiers do not reuse the synthetic catalog.
- **Validation:** current focused commands are `cargo test --manifest-path crates/aicharts-protocol/Cargo.toml --locked` and `bun test lib/usage`. Maintain property/fuzz seeds for round-trip canonicality, overflow, malformed/trailing input, duplicate identity, interval union, half-open boundaries and partial coverage. Add the remaining account/correction contracts and civil-time presentation tests before completing this phase.

## Phase 1: Shared identity and profile authority

- **Status:** Partial: browser registration, dormant server boundary, fresh-authentication coordination, browser custody, explicit live-account approval and internal retained-genesis recovery delivered. Public/CLI integration, live recovery qualification and profile extensions remain unfinished.
- **Depends on:** 0
- **Objective:** one Hraness identity works for web and device enrollment, with shared social/avatar profile.
- **Scope:** authority registration of AI Charts; supported CLI authorization flow; compatible SDK consumer/profile changes; GitHub normalization; avatar upload/finalize/delete; explicit public projection without email.
- **Out of scope:** private provider-token forwarding, independent AI Charts passwords, product-specific usage in shared profile package.
- **Acceptance:** issuer/audience/subject/scope checks fail closed; account rename preserves ownership; pairwise subjects map through canonical authority; old consumers remain compatible; profile revision conflicts are recoverable. Avatar dimension/byte/type/metadata protections and deletion races pass. CLI credentials are revocable and source-provider tokens never leave the machine.
- **Validation:** SDK focused `bun test src/identity/profiles.test.ts src/identity/consumers.test.ts`, existing auth tests and new media contract tests; full SDK `bun run check`, plus its required browser/release qualification. Authority owner records exact repository commands before implementation. Prove browser sign-in, native approval, revoked-device denial and wrong-user upload denial with isolated test identities. Publish the immutable shared release before application adoption.

## Phase 2: Local collector

- **Status:** Partial: strict local-only collection, ledger, explicit split-key shadow reindex and bounded native sender delivered. Explicit complete-prefix collection and credential custody are under integration. Richer measurements, native activation and installation qualification remain unfinished.
- **Depends on:** 0
- **Objective:** `aicharts usage` and `upload --dry-run` produce correct content-free measurements for Codex/Claude locally.
- **Scope:** selective readers, installed-version capability probes, incremental cursors, native identity/lineage normalization, numeric local ledger, account observations and exact dry-run projection. Live hooks/OTel ingestion remain local and opt-in.
- **Out of scope:** network publishing, desktop service installation, other providers, guessed historical human authorship/concurrency.
- **Acceptance:** no session files copied, no transcript objects persisted, no unnecessary token refresh; unrelated hooks/exporters preserved. Copied supported requests dedup across source paths; fork ambiguity is visible; streaming records count once; truncation/rotation/partial writes recover. Current plan is never retroactively assigned to unidentified history. Reader/uploader restrictions have a testable platform capability status, not an unconditional security claim.
- **Validation:** proposed `cargo test --manifest-path crates/aicharts-core/Cargo.toml --locked` and CLI fixture tests. Synthetic forbidden-field substitutions preserve measurement outputs and never retain forbidden content; bounded local cursor/transport bookkeeping may change. Malformed/oversized values fail boundedly without reflection. Test crash between cursor/ledger steps, resource bounds, deep nesting and unknown schemas. Provider compatibility fixtures record versions; actual installed-provider qualification reports only sanitized counts/capability results, never log content. Measure bounded CPU/RSS and incremental rescan work rather than asserting “fast” from language choice.

## Phase 3: Ingestion and storage

- **Status:** Partial source only: local tooling, staging, enrollment and namespace anchors are delivered. Dormant authenticated account admission is implemented with focused runtime evidence; its aggregate/protected delivery and all public/live activation remain gated.
- **Depends on:** 0, 1
- **Objective:** one authenticated enrolled user can upload/retry/query/delete measurements with correct durable recovery.
- **Scope:** Worker admission, per-user DO index, private R2 ledger/receipts, compaction, pricing snapshots, query authorization, queue/outbox, server materialization and deletion.
- **Out of scope:** public rankings, billing-provider credentials, live upstream benchmark dependency, unbounded query scans.
- **Acceptance:** raw request bodies never enter persistence/logging; only canonical records reach storage. Same sequence/hash commits once, conflicting retry fails. Copies on a second device dedup by user-scoped upstream identity, not device ID. Reinstall recovers the existing versioned dedup namespace; revocation/key rotation cannot remint ranked history. Correction can decrease totals. Explicit reserved/receipt-pending/accepted states fence concurrent revisions until their predecessor is durable. Accepted R2 receipts and tombstones reconcile after DO restore; orphan staging is excluded; delayed messages cannot resurrect deleted data. Gateway pricing is versioned, decimal-safe and coverage-aware; client money ignored.
- **Validation:** `bun run usage:worker:check` runs generated runtime types, separate strict TypeScript and local workerd tests; extend it for DO/R2/queue admission behavior. Inject failures before/after reserve, object write, finalize, receipt, acknowledgment and publication. Test weeks offline, retry exhaustion, simultaneous uploads, oversized body, unauthorized account, price gaps, compaction and restored-state replay. Run a bounded real upload and readback with explicit test identity; separately prove persistence and recovery. No production user-history import until the round trip and deletion tests pass.

## Phase 4: Dashboard and leaderboard

- **Status:** Not started
- **Depends on:** 1, 2, 3
- **Objective:** useful private analytics and explicit opt-in public rankings within existing AI Charts design/navigation.
- **Scope:** `/usage`, `/usage/me`, `/usage/settings`, `/leaderboard`, `/u/[username]`; daily contribution view, period/client/model filters, breakdowns, accounts/subscriptions, evidence/freshness/coverage states. Reuse immutable design primitives; preserve ordinary appearance control and accessible keyboard/touch behavior.
- **Out of scope:** public minute-by-minute schedules by default, “verified human” claims from role labels, calculator population claims.
- **Acceptance:** private routes cannot be cached or returned to another user; public projection excludes email/device/account aliases; ranking uses materialized bounded queries. No activity over 100%; two devices peaking at different times do not get summed peaks. Actual charges, API-equivalent value and fees have distinct labels. Empty/partial/stale/unknown-price/revoked/publishing-disabled states are usable.
- **Validation:** proposed `bun test lib/usage components/usage app/usage app/leaderboard app/u`; extend the repository's real app-verifier/browser harness with auth-separated fixture contexts and mobile/keyboard checks. Read installed Next.js docs before route implementation. Full `bun run check` after integration, including existing chart/calculator regressions.

## Phase 5: AI Charts skill

- **Status:** In progress; public benchmark retrieval and local-only numeric inspection implemented, not yet packaged or delivered
- **Depends on:** 2, 3
- **Objective:** an agent efficiently gets sourced benchmark answers and private usage summaries without touching raw sessions.
- **Scope:** portable skill instructions, bounded JSON CLI query commands, public snapshot cache/ETags and read-only personal query authorization. Use the skill-creator workflow when authoring the actual skill.
- **Out of scope:** implicit publishing, background enablement, credentials in prompts, full transcript analysis.
- **Acceptance:** benchmark response includes units/configuration/source/date/version; projections and pagination are bounded. Incompatible benchmarks are not collapsed into one score; stale/offline cache is labeled. Personal queries use numeric local data or authorized remote metrics. Ordinary analysis never changes upload/consent settings. External source text is treated as data, not instructions.
- **Validation:** proposed focused CLI skill integration tests and skill validation from its creator workflow. Scenarios: latest benchmark lookup, precise model filter, missing model, stale cache, incompatible score scales, personal hourly cost with missing prices, no login, expired read scope, malicious model/source text. Record response-byte savings versus full dataset without inventing a target before measurement.

## Phase 6: Background worker and menu app

The next release prerequisite adds memory-only canonical USTAR/gzip and exact manifest/checksum modules in `scripts/release/`, with explicit Node `release:archive:check` and `release:manifest:check` commands in the complete gate. Independent archive review caught pooled output backing buffers; a failing regression led to exact-sized unpooled copies. The accepted source is adopted byte-for-byte, with 84 integrated synthetic archive tests. A suspected regex edge case did not reproduce and required no source repair. Independent inventories constrain every member; exact inflation, path, mode, size, content, and ownership checks precede returned payloads. The independently reviewed manifest candidate passes 40 integrated tests and uses owned canonical serialization after a regression exposed inherited `toJSON` hooks. Its fixed Linux CLI/skill/source profile matches complete independently known expectations, never authenticates metadata obtained from the same untrusted artifact, and leaves BUILD bodies, licenses, executable compatibility, workflow attestations, and publication to separate gates. Both modules are pure byte boundaries, not filesystem extractors, publishers, or installers. Integrated source still requires final repository and Linux CI validation. Maintained references are `docs/usage-release-archives.md` and `docs/usage-release-manifest.md`.

CLI release prerequisite: the side-effect-free `--version` and `--version --json` implementation shipped in [PR 185](https://github.com/hraness/aicharts/pull/185), merged as `1dbd9649e1c1db4f90f83429f04db72cc48d696c`. It uses compile-time Cargo version and OS/architecture, with null source commit and explicitly unverified provenance. Mixed flags fail before other command dispatch, and synthetic process tests cover spoofed environment metadata and unchanged local canaries. The three source/test hashes and Cargo inputs match the independently accepted candidate. Its 24 unit/four integration tests, strict Clippy/format checks, final repository/KB gates, and exact-head/merge CI passed. Production `dpl_D1m2Ab11DDXpJoBsDuUjfxGyQsnn` matched the merge, with nine anonymous probes and a zero-error bounded log window recorded in the [delivery receipt](https://github.com/hraness/aicharts/pull/185#issuecomment-5642010138). No package publication, signing, installation, daemon, or upload was enabled.

- **Status:** Not started
- **Depends on:** 1, 2, 3
- **Objective:** explicit installation enables reliable low-overhead sync and a clear optional tray UI.
- **Scope:** absolute pinned binaries; LaunchAgent/systemd-user/Windows task integrations; lock/outbox/backoff/status/doctor; optional vector ai-circle tray; signed packaging/update rollback.
- **Out of scope:** always-on privileged system daemon, replacing the user's Tokscale automation, enabling disabled collection after repair.
- **Acceptance:** enable is idempotent and reversible; no overlapping manual/daemon readers; service status reports stale/errors without secret content. Offline/auth expiry preserves counters; sleep/wake/reboot/log rotation recover; pause/disable persists. Closing the tray and stopping collection are distinct. Updates verify publisher/artifact, replace atomically and recover interruptions. Each platform advertises only qualified collection/isolation capabilities.
- **Validation:** Rust platform fixture/unit tests plus actual macOS, Linux and Windows install/upgrade/uninstall/service restart smoke tests. Use the native/browser lanes for corresponding real application checks. Kill at each persistence/upgrade boundary, start simultaneous uploads, advance clocks, interrupt network and revoke auth. Measure idle CPU, RSS, wakeups and upload bytes on representative fixtures. Do not present Mac-only success as cross-platform verification.

## Phase 7: Trust, parity and calculator study

- **Status:** Not started
- **Depends on:** 4, 5, 6
- **Objective:** reviewed public launch with transparent moderation, group/sharing parity and useful optional utilization evidence.
- **Scope:** anomaly/review/appeal workflow; public/private groups; badges/embeds/yearly recap; study consent, billing-period coverage, privacy-preserving cohorts; separately reviewed Claude calculator dataset/scenarios.
- **Out of scope:** proof of productivity, universal anti-fraud, provider-cost/subsidy assertions from retail-equivalent counters, automatically replacing curated calculator anchors.
- **Acceptance:** quarantined/deleted/private data cannot leak through cohorts, badges or cached groups. No provider-verified badge without independently checkable evidence. Same subscription on two devices counts once; real separate subscriptions remain separate; plan changes are dated. Cohorts show sample size, selection bias, coverage and suppression. New calculator inputs retain old snapshots/source history and publish only after review.
- **Validation:** focused trust/cohort/share regression/property tests; cross-user copy attack, forged counts, clock changes, replay, re-enrollment, unknown prices, deletion-cache invalidation and sparse cohort privacy scenarios. Full current repository gates, independent security/privacy review and staged live release checks. Production verify canonical origin/commit, sign-in, known upload/readback, ranking freshness and deletion without altering unrelated production records.

## Delivery and recovery

Keep install/upgrade defaults local-only until the relevant online behavior passes qualification. Deploy additive protocols and backward-compatible readers before enabling new writers. New infrastructure stays disabled until owner identity, capacity, authorization, recovery and costs are verified. Use feature controls for public publishing/cohorts separately from private collection.

Before each merge, review task-owned diff, exact base/head, independent review findings and required CI. After merge, verify Vercel/Worker release identity and scoped live health; record commits, PRs, checks, immutable package/binary releases and production evidence in this plan. Roll back code/configuration to a compatible release without discarding acknowledged measurements. Reconcile pending operations rather than replaying blindly. No database reset, bucket purge or destructive migration is authorized by normal delivery.

## Resolvers before operational activation

| Question | Resolver and safe default |
| --- | --- |
| Which existing Cloudflare account/resources and cost envelope? | Integrator inspects authorized resources/current plans; reuse suitable owner-controlled resources, otherwise request only the missing budget/authority decision. |
| Which native authorization flow is supported? | Accounts owner verifies current authority; add reviewed support, never improvise credential extraction. |
| Can each platform enforce reader/uploader isolation? | Native owner provides capability tests; mark unavailable capabilities explicitly and keep stronger claims disabled. |
| Are historical prices/occurrence identities/plan periods available? | Collector/data owner tests versions and sources; preserve unknown/partial evidence rather than guess. |
| Is 16m intentional? | Preserve the explicit requested 16m view; optional later UX standardization is the product owner's choice, not a blocker. |

## Implementation log

### 2026-09-10 — local-only foundation, source validation passed

Added three Rust crates and matching TypeScript wire/rollup modules. A shared independently assembled golden frame establishes byte parity. Historical readers use metadata-only projections and keyed occurrence identities, retain unknown model/account/origin information, and do not fabricate intervals. The CLI requires explicit sources and a private namespace key; it can inspect or produce a local dry-run but cannot upload.

Independent review corrected non-object source metadata handling, optional Codex identity drift, pre-epoch timestamps, argument-encoding error reflection and Buffer aliasing. Coverage must be supplied authoritatively and exclude adjacent-day uncertainty. Per-request interval IDs are distinct from parent-agent IDs.

Integration review confirmed that both complete-gate CI callers install the pinned Rust toolchain and that data refresh remains fail-closed on setup or validation failure. The Next.js managed instructions remain intact in `CLAUDE.md`, with the mandatory installed-documentation rule retained in `AGENTS.md`; a regression checks that the pinned generator preserves both files and the KB guide format.

Remaining phase 0 work includes account-observation/correction/enrollment contracts, production registry/pricing dimensions and recovery state-machine implementation. Remaining phase 2 work includes local transactional cursors/outbox, credential-vault/recovery integration, live lifecycle telemetry and OS-enforced reader isolation. Current explicit limits and unsupported imports remain visible. No personal logs or credentials were used for tests; no live uploads or resources were activated.

The root integration owner passed `bun run check` with the pinned Rust toolchain: 57 Rust tests, 878 Bun tests, strict TypeScript, lint, generated-data checks, the 244-page production build and the real browser contracts. `bun run kb:refresh` and `bun run kb:check` also passed. Independent protocol, collector, server-contract and integration review findings are resolved. Exact-head Linux CI, merge and production verification remain delivery gates; the pull request will retain their immutable evidence.

### 2026-09-10 — foundation delivered; private ledger continuation

Foundation [PR 167](https://github.com/hraness/aicharts/pull/167) merged as `168af5bda89738f6c97d19dfab85ed69acc4c506` after integrating current main and passing the complete local gate (57 Rust and 887 Bun tests), independent review and exact-head [Linux CI](https://github.com/hraness/aicharts/actions/runs/34541493118). Vercel production deployment `dpl_GLuD7UKvizcuxiT6MSKUV3HvLPtF` matched that commit. Canonical home, calculator and data routes were healthy and the public coding-agent JSON matched the local route bytes; `/usage` and `/leaderboard` remained absent. The [delivery receipt](https://github.com/hraness/aicharts/pull/167#issuecomment-5626820546) records identity and readback. No binary release or operational usage activation occurred.

The next source slice adds a private numeric SQLite ledger, atomic complete-source snapshot checkpoints and a coalesced local pending outbox. Embedded SQLite is local-only: it does not provision Turso or replace the planned R2 server store. Changed sources are fully reparsed; unchanged file metadata can skip parsing, but open-time bounded integrity reconstruction still reads retained numeric state. CLI `init`, `collect`, `status` and `outbox --dry-run` expose this explicitly. They cannot upload, acknowledge, enroll, reset or migrate state. Account, model, human-prompt and live interval gaps remain visible rather than inferred.

Synthetic tests exercise an actual abruptly terminated pre-commit process with a hot rollback journal, subsequent recovery/retry, source regression, duplicate/streaming observations, revision conflicts, bounded pagination, schema corruption and forbidden-content canaries. Independent review found a macOS ancestor-alias issue before activation: resolve only the existing state parent before creating anything, while keeping final-directory and database symlink rejection. Aggregate validation and delivery evidence for this continuation belong in its eventual pull request; this entry does not claim them in advance.

The parallel identity audit inspected cached Accounts authority `hraness/accounts` at `fa2e4f7a01a365d07d11988ce89f70181ff6ec1a` and Suite Accounts SDK at `832e9d2364617b54b06872a81fab7a95791f4fd4`; remote freshness was not re-established during that audit. The authority supports browser authorization-code/PKCE and refresh grants, not device authorization; AI Charts is not registered. Plan an AI Charts-owned browser-approved device pairing flow with revocable upload-scoped credentials, never forwarding browser Hraness tokens to the daemon. Refresh the exact authority and SDK revisions before implementation and follow their independent release gates.

Shared profiles already contain several social fields, but GitHub, R2 avatars and a safe public leaderboard projection need compatible contract work. The existing private profile view includes email and must not be reused as a public response. Phase 1 remains unimplemented. Phase 2 still needs rotation/decreasing-correction recovery, credential custody, qualified incremental parsing, live telemetry and platform isolation; this local persistence slice is not the finished daemon.

### 2026-09-10 — ledger delivered; browser identity continuation

The private ledger slice shipped in [PR 170](https://github.com/hraness/aicharts/pull/170), merged as `43cd5e53a8633ed397dcedf33476af830f75d5e0`. The converged local gate passed 91 Rust and 889 Bun tests, browser contracts and KB checks; independent review and [exact-head CI](https://github.com/hraness/aicharts/actions/runs/34544826785) passed. Production deployment `dpl_52szpQGzGxZzTG8XHqjhyZ2vSVLn` matched the merge, and public coding-agent JSON matched the local route bytes. The [delivery receipt](https://github.com/hraness/aicharts/pull/170#issuecomment-5627266670) records readback. No usage upload, daemon or binary release was activated.

The identity continuation refreshed SDK main to `e3887c44fa57e79f7701bd45a45925e98fb96f9c` (v0.5.4) and Accounts authority main to `6f094896af73ffbb49dca5b14982682f5ebc5ed7`. SDK [issue 33](https://github.com/hraness/suite-accounts/issues/33) admits the current-only AI Charts production browser registration. Source review preserves frozen v1 identities, existing consumers and linked-product privileges, and rejects the new consumer in the old loopback helper. The authority change adds versioned OTP branding and synthetic exact-binding flow coverage; its client registry remains derived from the immutable SDK.

The independently reviewed [identity design](../../docs/usage-identity.md) separates source admission, runtime qualification and activation. The Accounts-owned change is implemented and reviewed before SDK publication. SDK source merge and immutable release precede authority release-tag adoption and independent application consumption. An attempted full-commit authority pin failed the existing tag-only dependency gate; review corrected our inferred merge-before-release ordering without changing or bypassing the gate. The AI Charts server boundary stays disabled by default and exposes no login UI, enrollment, ingestion or profile publishing. A valid factory binding does not prove deployed authority. Authority/application aggregate gates and production receipts remain pending until recorded in their delivery PRs.

SDK [PR 34](https://github.com/hraness/suite-accounts/pull/34) merged as `6e243744af9da76737248de08aaa604b5bcd85c2`. Local aggregate validation passed 214 tests and 9,093 assertions, package consumers, deterministic artifacts and boundary checks; local browser and exact-candidate CI passed. The independent [release workflow](https://github.com/hraness/suite-accounts/actions/runs/34548430082) passed and published immutable [v0.5.5](https://github.com/hraness/suite-accounts/releases/tag/v0.5.5) at that exact commit. Both consumers now use the released tag. AI Charts' focused auth suite passes 14 tests and 3,676 assertions with synthetic ES256, provider transport and cookies; no live login or upload was used.

Device design chooses AI Charts-owned browser approval with small durable control transactions and private R2 measurement blobs. Pairing uses independent short-lived polling and upload credentials, account confirmation in the terminal, retry-safe reservation and final-admission revocation checks. Fresh intent-bound approval is not provided by the existing session accessor. The current local key also controls source checkpoints and cannot be silently replaced with a recovered account namespace: a reviewed full reindex/migration must preserve retained history before ranked upload. No device state machine or recovery claim is implemented by this browser slice.

### 2026-09-10 — identity delivered; dormant pairing and staging source

AI Charts [PR 172](https://github.com/hraness/aicharts/pull/172) merged as `b427d72379eb72921f16e9986412c8cb2ff76d21`. The complete local gate passed 91 Rust and 903 Bun tests, generated contracts, strict TypeScript, lint, production build and real browser checks; independent review and [exact-head CI](https://github.com/hraness/aicharts/actions/runs/34551149919) passed. Vercel deployment `dpl_CZmz4JuMMeamqtQ9pJ877JmFBDeo` matched the merge. Public benchmarks remained healthy and the auth routes remained fixed `503` with no cookies. The [delivery receipt](https://github.com/hraness/aicharts/pull/172#issuecomment-5628150159) records readback. Accounts [PR 54](https://github.com/hraness/accounts/pull/54) deployed at `d2bc3718b23e5171647ddcfb982183ef875c1704`; guarded registry reconciliation added only the missing AI Charts client and the second pass made no changes. Its [receipt](https://github.com/hraness/accounts/pull/54#issuecomment-5628202730) records the exact authority and preserved historical registrations. No real login, daemon or usage upload was activated.

The next slice on `codex/usage-pairing-20260910` adds internal pairing transitions only through terminal confirmation and canonical numeric R2 staging only. This narrows the proposed backend slice deliberately: the existing SDK does not expose verified `auth_time` plus sealed intent completion, and the legacy ledger cannot be rekeyed from stored numeric IDs. Account enrollment, stable namespace recovery/reindex and accepted ingestion remain separate prerequisites. [Usage Worker boundaries](../../docs/usage-worker.md) owns the implementation and activation limits.

Wrangler `4.131.0`, the Cloudflare Vitest plugin `1.1.7`, Vitest `4.1.11` and workerd `1.20260910.1` are locked for local tests. Runtime types were generated successfully. Independent review found that Wrangler prefers a legacy user configuration directory over XDG; the runner now refuses that path using an existence-only check, strips inherited credentials and refuses environment files in the fixture directory. No personal configuration is read or moved by that guard. Source tests include durable restart/expiry/retry/account-binding cases, canonical stream bounds, conditional-write conflicts and uncertain-write readback. They are not production PITR or live enrollment evidence.

Worker strict TypeScript, focused lint, diff checks and the three runner tests (25 assertions) passed again after the final source additions. Independent source review found no remaining defects after the legacy configuration guard, runtime input guards and committed-write/readback-loss regressions were added. Host runtime validation was rejected because the automatic approval reviewer hit its usage limit. The task did not bypass the scheduler or approval, and no aggregate gate, PR, merge or deployment is claimed for this slice. The managed baseline check also refuses the pre-existing mixture of HRA/Oompa markers; preserve that guarded exception until the shared adoption tool has a supported repair path. Source remains uncommitted pending runtime checks and full repository/KB gates; source review is not runtime qualification.

After quota restoration, the user explicitly approved the execution tool's required shell transport while preserving the absolute host scheduler and every validation gate. The first real workerd run exposed 31 failures: unsupported SQLite `PRAGMA user_version` prevented pairing bootstrap, and R2's own optional `undefined` HTTP metadata fields caused valid staging readback to fail. The repairs moved schema versioning into the strictly checked singleton row and admitted only documented absent HTTP fields. Unknown schema versions, missing initialized rows, unexpected tables/indexes, populated or unknown HTTP metadata, changed canonical bodies and conflicting immutable objects still fail closed. Independent review confirmed those limits. Focused pairing passed all 20 tests; all 68 staging tests passed, including real conditional writes, lost replies, late-result cleanup and independent body verification. Worker strict TypeScript and focused lint passed after the repairs. Full integration and exact-head delivery gates remain required.

### 2026-09-11 — pairing delivered; sealed authentication coordination

Pairing and staging [PR 173](https://github.com/hraness/aicharts/pull/173) merged as `a1985cdf27980bbb4dc480ae42ec78a4744e0338`. The final local gate passed 91 Rust, 89 Worker and 906 Bun tests, production build and browser contracts. Independent review and exact-head CI passed. Production deployment `dpl_A8eSqMv3Ece6R2TBTtqLtRKpwbXy` matched the merge; nine canonical probes verified unchanged benchmark JSON and dormant authentication. Its [delivery receipt](https://github.com/hraness/aicharts/pull/173#issuecomment-5628748985) records the evidence. No Worker or R2 resource was activated.

Suite Accounts [PR 36](https://github.com/hraness/suite-accounts/pull/36) added explicit fresh authentication with server-owned sealed context. It merged as `453ce96c694a92d51482277d898eb7f1b3cca3c5` and published immutable [v0.6.0](https://github.com/hraness/suite-accounts/releases/tag/v0.6.0) after its own full package/browser gate, independent review, exact-head CI and [release workflow](https://github.com/hraness/suite-accounts/actions/runs/34560117306). Local SDK validation passed 296 tests / 10,760 assertions, including 82 focused fresh-authentication tests. The artifact does not approve product actions or claim exactly-once consumption.

The AI Charts continuation consumes that immutable tag. Its server-only coordinator starts an owned durable attempt, encodes the exact proof under a versioned context domain, consumes the real SDK completion internally and records authentication before releasing the continuation. It never exposes caller-constructible verification evidence, invokes approval or connects a public route. Ordinary route behavior is unchanged. The narrow intent resolver remains unconfigured; authenticated Vercel-to-Worker transport is a later boundary, not a mock presented as live connectivity.

Impact review traced the dependency through the ordinary auth handler, encrypted transaction/session cookies, the new pairing coordinator and the existing durable recording DTO. The SDK keeps ordinary transaction v1 compatible and fresh v2 fail-closed on ordinary callbacks and old-package rollback. No provider registry, browser callback address, local ledger, wire frame, R2 staging, public profile, benchmark dataset or calculator contract changes. A recorded attempt is reversible only by its existing expiration/denial/supersession lifecycle; deployment must not reset durable or local user state. Public auth remains disabled throughout source delivery.

Independent review tightened per-operation clock monotonicity and original-cookie-secret checks after asynchronous work. Failure or response loss withholds the successful session cookie and continuation, even if the durable record may have committed. Synthetic SDK and local Durable Object tests remain distinct evidence layers. Provider fresh-OTP semantics, browser nonce custody, explicit same-origin/live-account approval, authenticated transport, enrollment, recovery/reindex and live upload qualification remain unfinished. The managed baseline check still refuses mixed HRA/Oompa markers; its guard was not bypassed. Focused and aggregate integration results belong in the delivery receipt after they pass.

### 2026-09-11 — fresh coordinator delivered; protected browser approval source

Fresh coordination [PR 174](https://github.com/hraness/aicharts/pull/174) merged as `398deb57f16c1fa94587ea9e013a4752b4011a4e`. The complete local gate passed 91 Rust, 89 Worker and 937 Bun tests, generated contracts, strict TypeScript, lint, production build and browser checks. Independent review, [exact-head CI](https://github.com/hraness/aicharts/actions/runs/34561362391) and CodeQL passed. Production deployment `dpl_4rZVh196NtSy3mgY9m4z8znFutJG` matched the merge; nine canonical probes verified unchanged benchmark JSON and dormant authentication. The production auth flag remained absent. The [delivery receipt](https://github.com/hraness/aicharts/pull/174#issuecomment-5629413693) records exact identity and readback. No live login, device enrollment or usage upload occurred.

The continuation on `codex/usage-browser-approval-20260911` adds one protected cookie per browser attempt, with server-generated independent nonce and CSRF tokens. The cookie has fixed binary fields, purpose-separated authenticated encryption and the original bounded expiry. Fresh completion must match its proof to the SDK-sealed context. Separate approval read/decision methods require exact same-origin requests, a matching live Accounts session and recorded fresh authentication. The canonical bounded POST verifies CSRF before remote work. Responses project only the account, state, earliest expiry and stable CSRF, never full attempt proof or authentication bearers. The cookie remains available for uncertain-decision readback until its original expiry.

Durable `browserStatus` and `decideBrowser` replace the old consent methods with explicit readback and an atomic live-account/expiry check. Stale proofs in these two methods cannot exhaust the terminal guessing budget; authentication recording retains its existing budget. Identical live decisions are idempotent, and denial cannot undo terminal confirmation. Independent review corrected a zero-byte chunk loop that could starve the body timeout and retained the minimum cookie/authentication deadline through the final recording check. Focused local workerd validation passed all 36 pairing tests, including seeded transitions, restart and lost-response readback. The real-SDK auth suite passed all 79 tests and 34,488 assertions, including immediate empty-chunk cancellation and a five-second stalled-body limit. Worker strict TypeScript, focused lint and whitespace checks passed. Aggregate validation and delivery remain required until recorded in this slice's delivery receipt.

The public auth handler, routes and production fence remain unchanged. Callback mode dispatch and ordinary-start custody cancellation need explicit integration before browser activation. No Worker transport, account enrollment, upload authorization, wire format, local ledger, public profile, benchmark or calculator data changes. Managed adoption still refuses the existing mixed HRA/Oompa markers; no manual marker repair or guard bypass occurred. Current behavior and activation prerequisites are maintained in [Usage identity](../../docs/usage-identity.md) and [Usage Worker boundaries](../../docs/usage-worker.md).

The next enrollment design should use account-owned durable control state. A terminal-confirmed view alone lacks a reserved enrollment and must not grant a namespace. Verify the original polling secret and committed upload-secret preimage, then reserve an exact operation bound to the confirmed account. An account-owned transaction should check the live reservation and an external recovery generation, create a stable namespace only for a genuinely absent account, and atomically record the device and immutable retry receipt. Reconciliation of pairing consumption must not become a second authorization commit. Retain local operation secrets through lost replies; revoked devices must never reactivate on retry. This is proposed work, not an implemented enrollment API.

The local key currently fingerprints both source checkpoints and occurrence IDs. An account namespace therefore needs an explicit shadow-ledger reindex, with separate local-checkpoint and account-occurrence keys. Preserve the old key, database and outbox until the replacement is verified. Re-read selected native sources; unavailable historical identities stay unresolved and local-only. Hashing old opaque IDs again, resetting the ledger or acknowledging all legacy history cannot establish account-wide deduplication.

### 2026-09-11 — browser approval delivered; account enrollment source

Browser custody and approval [PR 175](https://github.com/hraness/aicharts/pull/175) merged as `ced8f0e62bc0dfa963f5c4dd23ca777878c33e62`, with reviewed and merged tree `e6b964c2023cf34eb3f6bb437f4986bb6a5903ef`. The complete local gate passed 91 Rust, 105 Worker and 971 Bun tests, generated contracts, strict TypeScript, lint, the 244-page build and browser checks. Independent review, [exact-head CI](https://github.com/hraness/aicharts/actions/runs/34563872055) and CodeQL passed. Vercel production `dpl_2BKHKMRrP52fQ5YPvvL5sCRhwPvy` matched the merge; nine canonical probes verified unchanged benchmark data and dormant authentication. The auth flag remained absent. The [delivery receipt](https://github.com/hraness/aicharts/pull/175#issuecomment-5629737780) records identity and readback. No usage Worker, login, enrollment or upload was activated.

The continuation on `codex/usage-enrollment-20260911` implements the internal enrollment boundary. Pairing verifies both original preimages and retains one immutable reservation under the minimum original deadline. Account enrollment resolves that grant directly through the Pairing binding and checks its canonical account object and external recovery generation. It stores a pending random namespace before conditional R2 creation and exact readback, then commits the device and non-secret receipt together. A receipt retry never reactivates revocation; namespace disclosure separately requires live authority and a final revocation check. A lifetime device cap retains revoked identities rather than evicting them. Pairing v1 state migrates only after full validation; unknown and corrupt state is retained unchanged.

The selected namespace design uses a 160-byte immutable R2 anchor plus account-owned durable pending state. DO-only storage lacks independent namespace-existence evidence after an object restore. A fleet-wide derivation secret would couple all account identities to permanent key custody without solving revocation recovery. The chosen design does not make arbitrary restores safe: empty account state with an existing anchor closes, a missing active anchor is not recreated, generation mismatch never remints state, and simultaneous administrative erasure cannot be detected without the required external operational fence. An expired unfinished genesis remains `recovery_required`; fresh pairing cannot silently adopt it. [Usage Worker boundaries](../../docs/usage-worker.md) owns those current limits and the still-unimplemented reconciliation procedure.

Independent review tightened clock monotonicity across asynchronous work, exact persisted device-ID derivation and retention of the genesis receipt. Real DO-to-DO testing exposed the runtime's added disposal symbol; the repair handles it only on the trusted RPC response, leaving external proofs and nested reservation DTOs exact, and disposes immediate and late replies. Synthetic tests cover real local DO/R2 calls, lost conditional-write responses, retries, revocation, expiry, generation changes, corruption and privacy projections. Focused and aggregate receipts will be recorded after convergence; source review does not establish live recovery or provider qualification. The supported baseline check still refuses pre-existing mixed HRA/Oompa markers; no manual marker repair or guard bypass occurred.

The parallel local-ledger audit found that `Ledger::open` is read-write and can recover a hot rollback journal, even when used for status. A genuinely read-only reindex plan needs an explicit inspection path that refuses recovery-required state. The next local slice should preserve the old database, outbox and key; re-read selected native sources into a separately created shadow ledger; and bind both local-checkpoint and account-occurrence keys. Existing opaque IDs cannot reconstruct the native account-scoped identities. Missing sources must remain unresolved, never silently acknowledged. No Rust files, personal sources, credentials, daemon settings or provider resources were changed in this enrollment slice.

### 2026-09-11 — enrollment delivered; continuous end-to-end completion

Internal account enrollment [PR 176](https://github.com/hraness/aicharts/pull/176) merged as `6facfd51ba07bcd50a36ad87380cc7e8c8f15aa3`, with exact reviewed/merged tree equality. Independent source, test and documentation review passed. The full local gate passed 91 Rust, 200 Worker and 971 Bun tests, generated contracts, strict TypeScript, lint, the 244-page build and browser checks. [Exact-head CI](https://github.com/hraness/aicharts/actions/runs/34616251031), [CodeQL](https://github.com/hraness/aicharts/actions/runs/34616246378), protected integration and [post-merge CodeQL](https://github.com/hraness/aicharts/actions/runs/34616769172) passed. Production identity and nine read-only canonical probes passed; auth and usage activation remained disabled. The [sanitized delivery receipt](https://github.com/hraness/aicharts/pull/176#issuecomment-5636879641) retains public evidence without provider identifiers or proof material.

Work continues on `codex/usage-e2e-20260911` from that exact merge. Preserve the current native key/database/outbox while preparing account-scoped replacements; do not inspect private sources merely to author or test the implementation. After the reindex boundary, close enrollment/recovery and authenticated admission, then integrate the requested dashboard, shared profiles, benchmark/private-usage skill and opt-in background installer. Qualification must prove actual sign-in, upload/retry/revocation/deletion and a recoverable installation on each advertised platform before enabling the corresponding capability. Existing benchmark/calculator data and unrelated Accounts work remain protected throughout.

### 2026-09-11 — recovery delivered; bounded sender and journal custody

Shadow reindex and retained-genesis recovery [PR 177](https://github.com/hraness/aicharts/pull/177) merged as `7a821922a54ee7138e390d58b8f913b98d2e115d`, with exact reviewed/merged tree equality. Independent review and the full local gate passed: 121 Rust, 221 Worker and 971 Bun tests, generated contracts, strict TypeScript, lint, production build and real browser contracts. [Exact-head CI](https://github.com/hraness/aicharts/actions/runs/34620788015), [CodeQL](https://github.com/hraness/aicharts/actions/runs/34620785300), protected integration and [post-merge CodeQL](https://github.com/hraness/aicharts/actions/runs/34621410700) passed. Production identity and nine canonical read-only probes passed; auth and usage activation remained disabled. The [sanitized delivery receipt](https://github.com/hraness/aicharts/pull/177#issuecomment-5637583080) records public evidence without provider identifiers or proof material.

The continuation on `codex/usage-admission-20260911` freezes a shared Rust/TypeScript operation and terminal-journal format. One immutable batch carries 1–256 operations, bounded to 82,024 bytes, with a matching terminal journal bounded to 67,744 bytes. A 100,000-occurrence history needs roughly 391 batches, not one network transaction per occurrence. This is a request-count bound, not a measured throughput claim. Rejected batches consume their whole sequence range but never acknowledge measurements; conflicts and deletion require explicit reconciliation and revocation gates the sender. The local schema migration is explicit and preserves existing measurements/checkpoints/outbox. It retains the latest exact request/journal pair for lost-reply readback. Ordinary inspection never migrates or acquires authority.

Internal R2 helpers own and validate canonical bytes before storage, conditionally create immutable batch and revision-addressed journal objects, and verify actual readback bytes, checksum and exact metadata. They are not an acceptance index or authorization boundary. Rust and TypeScript consume independently assembled synthetic byte fixtures. Independent review found and corrected TypeScript caller-controlled array iterator/accessor and typed-array proxy handling. Focused TypeScript parity/boundary tests passed 21 tests / 2,183 assertions; the Worker gate passed 232 tests, including 11 new immutable object tests. Protocol tests passed all 34 cases; native sender tests passed 61 cases with one explicit performance probe ignored in ordinary runs. Independent source review accepted the converged protocol, object and sender boundaries; aggregate integration remains required before delivery.

Measured native sender cost exposed repeated full reconstruction at the 100,000-occurrence retention boundary. A transaction-scoped scalar audit stamp preserves full-open/changed-state audits and exact schema/control validation while bounding unchanged-state record validation to at most 768 IDs. Review and tests cover unselected same/external writes, schema changes, actual failed COMMIT, callback rollback and the commit-to-publication race. The unchanged synthetic release-mode probe improved warm 256-member freeze/settlement pairs from 3,246–3,394 ms to 61 ms. First freeze still audited the full state in 638 ms; full open took 622 ms, and database bytes stayed 119,730,176. This is one-machine local work on a live handle, not a measured whole-history upload or a network guarantee. Scoped Clippy and formatting passed.

Authenticated admission must extend the existing account DO, retaining enrollment's bounded JSON separately from compact SQL subject/sequence indexes and R2 measurement/journal blobs. Routine uploads verify only the retained device's original upload commitment and active generation, not the expired enrollment grant or polling secret. Preserve accepted heads while receipt persistence is uncertain; atomically publish only after exact readback. Revocation after an irreversible decision cannot alter that decision. One contiguous immutable account journal must also cover control events, with a globally closed external recovery fence. These server authority/index/restore operations are not implemented by the codec/object helpers. Current rollup capacity, complete-prefix collection, credential isolation and all live activation requirements remain open.

### 2026-09-11 — sender delivered; native custody and explicit public profiles

Bounded batch/journal custody [PR 178](https://github.com/hraness/aicharts/pull/178) merged as `a13a28728ff3c73b5f852ebf95d1202a595fd40e`; the reviewed and merged tree is `96214f3632d1f35f80c5c0611b6a6a732581f1ef`. The full local gate, independent review, [exact-head CI](https://github.com/hraness/aicharts/actions/runs/34628054025) and CodeQL passed. The local gate included all workspace Rust checks, 232 Worker and 992 Bun tests, strict types/lint, generated contracts and the production build/browser gate. Production identity and nine canonical probes verified unchanged benchmark data and dormant auth/usage routes. The [sanitized delivery receipt](https://github.com/hraness/aicharts/pull/178#issuecomment-5638451246) retains public evidence without provider identifiers or proofs. Publishing these libraries activated no upload or daemon.

Suite Accounts [v0.7.0](https://github.com/hraness/suite-accounts/releases/tag/v0.7.0) is immutable and independently delivered. Its exact public profile DTO excludes email/authentication names, adds GitHub and opaque avatar references, and separates private edits from explicit publication. Accounts adopts that release and adds owner-derived optimistic mutations, revision-bound consent and a fresh anonymous public projection. The public HTTP layer reparses the exact DTO and username, forwards no browser credentials, disables caching and bounds upstream bytes/time. Image rendering, R2 upload/serving and publication UI remain unimplemented by that authority slice. AI Charts does not duplicate the shared identity authority.

Native continuation on `codex/usage-native-custody-20260911` adds explicit schema-1→3 / schema-2→4 complete-prefix migration and distinct `prefix-enable` / `collect-prefix` commands. Full prefix replay verifies a domain-separated local HMAC over the same bytes fed to the parser. Partial JSON/UTF-8 tails wait for LF; previously nonempty unwitnessed history cannot disappear behind tail deferral. Unchanged-witness replay must reproduce the exact prior frame set, suffix shrink never crosses a retained completed prefix, and old collection APIs refuse prefix layouts. Metadata-only skipping remains a reliability optimization, not tamper attestation. Reindex remains strict and cannot auto-promote or migrate state.

The new credential library has purpose-separated nonformatting secret types, bounded binary records and create-only macOS Keychain operations with exact readback and conservative uncertain outcomes. Pure reference-manifest work retains exact prepared intent and record commitments before a vault write, with revision/digest CAS and no automatic replacement. Its real filesystem entry points remain disabled pending descriptor-based ownership/ACL/durability qualification. No live keychain was accessed by tests; neither syntax validation nor a local commitment proves authenticated enrollment or rollback resistance. CLI vault integration, signing/upgrade behavior, reader/uploader isolation and all live installation claims remain open. Focused reviews and regression evidence feed the required aggregate; they do not replace it.

### 2026-09-11 — private native filesystem qualification

Complete-prefix collection and the custody engine [PR 179](https://github.com/hraness/aicharts/pull/179) merged as `bacbb4921592073b4036c579e3738a8ce3a8bae8`. Required CI, independent review, protected integration and the production identity/readback passed; the [public receipt](https://github.com/hraness/aicharts/pull/179#issuecomment-5639498896) records scope and limitations. Accounts' public profile authority is separately delivered, and Suite Accounts [v0.8.0](https://github.com/hraness/suite-accounts/releases/tag/v0.8.0) adds its explicit publication editor. Accounts' adoption adds server-side session/account preconditions, not a second identity authority.

The private macOS filesystem adapter accepts only an already trusted directory descriptor. It pins directory and permanent lock identities, requires current-user ownership, strict modes, no ACL entries and local writable APFS with ownership enforcement. It uses bounded descriptor-relative I/O, immutable staging, predecessor-bound envelopes, no-replace initialization and full synchronization without weaker fallback. The narrow C ACL bridge checks complete file-security metadata before accepting absence; synthetic allocation-failure cases cover the platform's ambiguous-success behavior.

Independent review found two durability-continuity races: replacing current with byte-identical bytes after its file sync, and changing it during a subsequent pending-file read. The repaired implementation retains the synchronized inode and envelope through directory sync/final readback and revalidates both file observations after nested reads. The focused macOS adapter gate passed 37 cases, including real disposable named replacements, process lock contention, retained-candidate retries and pre-vault fault ordering; strict Clippy passed. ACL tests separately passed seven cases, including the C fault matrix and disposable real files. These receipts precede the required aggregate gate.

The public reference store remains closed. Trusted anchor discovery, actual disposable Keychain qualification, CLI integration, installation and all online activation are still required. No personal files, session logs, credentials or keychains were accessed, no daemon was installed, and no upload service was activated by these tests. Checks detect observed replacement and coordinate cooperating processes; they do not establish hostile-owner isolation, rollback resistance or power-loss proof.

### 2026-09-11 — native storage delivered; qualified anchors and admission next

Private native storage [PR 181](https://github.com/hraness/aicharts/pull/181) merged as `5e75dc1d66226880541832f89df0769986947d1d`. Independent review, the full local gate, exact-head checks, protected integration, post-merge CI and CodeQL, production identity and nine canonical probes passed. The [delivery receipt](https://github.com/hraness/aicharts/pull/181#issuecomment-5640268018) records the source and live evidence. The public credential facade and usage service remain disabled.

Accounts [PR 56](https://github.com/hraness/accounts/pull/56) separately delivered the immutable SDK's public-profile editor with server-verified session/account preconditions. Required checks, the synthetic real-component browser gate, protected integration and six fresh anonymous production probes passed. Its [receipt](https://github.com/hraness/accounts/pull/56#issuecomment-5640612100) distinguishes that evidence from still-unqualified authenticated owner and image flows. No profile migration or avatar writer shipped.

The native continuation validates an existing absolute anchor through retained descriptor-relative edges, then opens the private vault lazily after durable manifest preparation. Its disposable Keychain fixture must use an exclusively created, validated directory and an explicit private keychain, with prompting disabled. Timeout or abnormal child completion retains the fixture for recovery rather than assuming descendant processes have exited. Focused checks and independent review precede live qualification; source delivery must not activate unqualified defaults.

Converged focused checks passed 111 custody tests and nine ACL tests, plus strict all-target Clippy. Independent review accepted the final process-custody repair. One explicitly admitted disposable Keychain run then passed, exercising the private synthetic vault without selecting the login keychain or changing search lists. The owned fixture was removed after successful child completion and identity readback. This qualifies that mechanism only; signed application, default-keychain, upgrade, CLI and daemon qualification remain open. The final integrated tree includes the newly merged, non-overlapping editorial PR 180 before its aggregate gate.

The owner-controlled private R2 resources are now provisioned, with public URLs disabled and no custom domains. No objects, production bindings, Worker deployment or new credentials were created by provisioning. The local Wrangler configuration remains a synthetic fixture. Resource creation does not qualify data persistence, disaster recovery or service authority.

Authenticated admission will first remain internal to the existing account object. Reserve one bounded account-wide flight, freeze its irreversible terminal decision after immutable batch readback, and publish all accepted heads only after the exact revision-addressed journal is verified. Retain original device-upload commitments and separate journal revisions from enrollment revisions. Unknown state, conflicting immutable objects, exhausted capacity and restored state without an external recovery fence must stay closed. This source work does not yet exist in the object helpers.

The current collector supplies partial token totals, not the requested full metric set. It emits unknown model/account/authentication attribution and no qualified human-prompt or execution intervals. Complete-prefix profile 1 must retain its exact replay semantics. Add source-qualified local observations in a separate versioned step before extending ledger/admission data: preserve unknown origin, unavailable coverage and clock uncertainty instead of inferring human prompts or active agents from transcript text. Existing rollup helpers can compute covered intervals, but no production collection path supplies that evidence yet.

### 2026-09-11: source-free inspection and benchmark skill

The continuation on `codex/usage-inspection-skill-20260911` adds a separate `inspect` CLI command over the existing `ReadOnlyLedger` API. It validates all options before file access, selects legacy or explicit split-key identity without guessing, refuses recovery sidecars and returns only a numeric summary. Revision and token totals remain decimal strings. The snapshot is rechecked before rendering; no source paths, IDs, witnesses, frames or keys enter its output. Synthetic tests cover all four ledger layouts, retained frozen uploads, unchanged bytes/directory entries/nanosecond metadata, unsafe paths, wrong keys and fixed errors. Ordinary access timestamps may change; hostile-owner isolation and rollback resistance are not claimed.

Codex sources with absent or null examined reasoning counters now receive the existing `unmeasured_reasoning` warning. Numeric fallback, occurrence identities, complete-prefix profile and wire bytes remain unchanged. Warning-only replay may update warning metadata and the ledger revision but preserves exact prefix witnesses, measurements and sender state. Metadata-skipped sources do not acquire retroactive warnings until a later reread. Focused red/green collector and ledger regressions and independent source review passed.

The portable `skills/aicharts` entry point separates anonymous public benchmark discovery from explicit local inspection. Its dependency-free helper bounds origin, bytes, pages and schema, preserves configuration/units/version/source/coverage, and checks catalog/dataset correspondence. It hashes fetched bytes for consistency, not source authentication or atomic publication. Independent review found a fetched top-level property collision with helper provenance; strict envelope keys and explicit projections repair it, with red/green collision and exact-hash controls. The helper never reads local usage. Fresh local mode requires an already available reviewed inspect binary, selected private state and key paths; it does not install, collect, recover, enroll or upload.

No remote personal query, hourly usage, model-price attribution, benchmark cache or signed native distribution is introduced. Public usage routes and native custody remain closed. Candidate-focused tests and review are complete; root-owned integration, repository/KB gates, exact-head CI and production readback remain required before delivery. The existing mixed HRA/Oompa baseline markers remain a supported-tool refusal rather than a manual repair.

### 2026-09-11 — inspector/skill delivered; internal account admission

Source-free inspection and the portable benchmark/private-usage skill [PR 183](https://github.com/hraness/aicharts/pull/183) merged as `f68bb01c1113bbcb11270bab4d956ccb3c21dbbb`, with identical reviewed/merged tree `d616350fe74e5b1409dd01e55d91ba76b2f48cd0`. Independent review, exact-head and exact-merge CI/CodeQL passed. Nine anonymous production probes verified exact deployed identity, unchanged public benchmark bytes and dormant auth/usage boundaries; the production activation flag remains absent. The [delivery receipt](https://github.com/hraness/aicharts/pull/183#issuecomment-5641296722) records the bounded result. This is not a signed, installed CLI or personal-skill installation.

The continuation on `codex/usage-account-admission-20260911` composes the existing enrollment authority and immutable helpers into one internal two-field upload RPC. Schema 3 adds five exact SQL tables atomically, preserving validated enrollment state, all retained device identities and namespace anchors. One account flight reserves sequence capacity before object effects, freezes a canonical terminal decision after batch readback, then atomically publishes all heads/day counts and sequences only after journal readback. Exact retries, revoked-before/after-decision semantics, one-predecessor help, immutable-object quarantine and shared durable clock floors are explicit. No public route, binding, provider configuration or runtime activation changes.

The initial source policy keeps registry/profile 1 immutable across retained retries and rejects future measurements before reservation or predecessor-help effects. It retains 100,000 subject identities including tombstones, 65,536 live heads per day and 4,096 terminal revisions. Capacity is checked as a net whole-batch projection; tombstones release live-day capacity, not identity. Startup audits canonical bytes, visible journal time order, head/device sequence ownership, retained receipt relations and every pending decision against its unchanged predecessor. These finite local checks do not authenticate missing history, attest provider usage or make administrative restore safe.

Independent source review found and repaired four subtle boundaries: future input before predecessor help, mutually ordered retained journal times, sticky material fence errors despite concurrent completion, and impossible zero-predecessor tombstone heads. Test review strengthened canonical corruption cases, stale-response/successor races, requesting-device revocation while helping, authority quarantine and rollback after every publication phase. The first full Worker check passed types and 47 new admission cases, including a real 100,000-head constructor audit and the 4,096-revision cap; its one failure was an old enrollment assertion that omitted the newly required durable clock observation. The repaired assertion preserves exact other state and checks both floors; all 79 enrollment tests then passed. Final focused admission validation passed 53 cases, including independent CAS outcomes, isolated invalid outcome 6, mixed journal ownership and four device-lifetime contradictions. Independent source, test and documentation reviews accepted the repairs. The root aggregate and protected delivery remain required. Managed baseline adoption still refuses mixed HRA/Oompa markers; no manual bypass occurred.
