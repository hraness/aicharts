# Usage Worker boundaries

The Usage Worker contains device pairing, account enrollment, numeric staging, authenticated batch admission, private daily reads and a separate public leaderboard. With its production flags unset, the public handler returns a fixed, private `503`. The source fixture defaults and production configuration differ: [Usage activation](usage-activation.md#recorded-production-evidence) owns the dated deployment inventory and cutover evidence. Read that inventory before provisioning or changing a deployed resource. A separate [private synthetic qualification procedure](usage-cloudflare-qualification.md) exercises internal operations through a named service entrypoint. Local test results and source admission do not establish live qualification or product activation.

## Storage split

Pairing, enrollment and admission require transactional, durable control state. SQLite-backed Durable Objects provide that state without adding a separate SQL service. Private R2 holds compact measurement batches, terminal journals and a separate fixed-size account namespace anchor. Pairing has its own object. The account object retains enrollment JSON separately from bounded binary admission tables, so decisions, revocation and publication share one synchronous order.

Cloudflare recommends SQLite-backed [Durable Object class exports](https://developers.cloudflare.com/durable-objects/reference/durable-objects-migrations/). The local configuration declares `PairingIntent`, `AccountEnrollment`, `RestoreFence` and `LeaderboardIndex`, separate `STAGING` and `CONTROL` R2 bindings, and a synthetic recovery-generation value. It disables public Worker/Preview URLs and observability and contains no production account, route or remote test binding. It is a local fixture configuration, not a qualified deployment target.

Private Standard R2 buckets `aicharts-usage-records` and `aicharts-usage-control` were provisioned on 2026-09-11 in the existing owner-controlled account. Readback confirmed disabled public development URLs and no custom domains. That provisioning inspection found no deployed Worker bindings or qualified service authority for those buckets, and provisioning uploaded no objects. The later production inventory in the activation runbook records different existing bound resources. The separate shared-profile media bucket belongs to Accounts. Do not deploy the local fixture configuration against these resources; production identity, bindings, recovery and live data checks remain required below.

The qualification templates under `services/usage-worker/qualification/` also remain unconfigured in source. The driver creates private, exact-target configurations for one synthetic account and a finite operation sequence. It records deployment checkpoints separately and never deploys from a test command. Follow the linked procedure for provider readback, ambiguous operations and generation closure.

## Pairing authority

Terminal confirmation does not create an account namespace, enroll a device or activate its committed upload credential. A separate reservation operation requires both the original polling secret and upload-secret preimage. These secrets have distinct hash domains and must be independent. Persisted pairing state contains commitments, bounded opaque identifiers, timestamps and counters, never preimages, OAuth bearers or transcript fields.

The browser attempt has a random context capability and nonce commitment. The server-only coordinator in `lib/usage/pairing-auth.ts` carries the exact proof inside the SDK's sealed transaction and a separately protected browser cookie. Fresh completion consumes the verified account and signed `auth_time` internally and calls only `recordVerifiedAuthentication`. The production auth server connects its narrow transport through `createVercelPairingTransport`, behind the authentication and pairing controls. `prompt=login`, a recent token `iat`, a callback response or the existing product session are not sufficient freshness evidence. Ordinary login and refresh cannot produce pairing authentication or approval. A successful recording is itself the browser approval: the durable transaction moves the attempt from `pending` to `browser-approved` for the verified account, and the terminal still confirms the same account before any reservation.

Only the trusted coordinator may record verified authentication through the guarded server transport. Internal method validation checks its bounded DTO and timestamp relationships; it cannot verify an OAuth signature or manufacture trust from a caller-supplied timestamp. The coordinator maps the SDK's earliest transaction/token expiry to `sessionExpiresAtMs` and withholds its continuation when recording fails or becomes uncertain. Durable recording is idempotent for identical live evidence but never grants consent.

`browserStatus` returns a bounded view for the exact current browser proof. The internal view includes expired or terminal state for reconciliation; the HTTP coordinator exposes account state and CSRF only while the cookie, fresh evidence and matching live account session remain valid. `decideBrowser` replaces the earlier internal `approve` and `deny` methods; deny is the only remaining live browser decision. Its atomic transaction checks that the account matches and both recorded authentication and the supplied live-session expiry remain current, then applies the explicit decision. Exact live retries preserve the decision, denial cannot undo terminal confirmation, and stale proofs in these two methods do not consume the polling-secret guessing budget. Authentication recording retains its existing proof-failure budget. Inputs are copied before asynchronous hashing so caller mutation cannot change an admitted operation.

The application implements protected cookie custody, same-origin and CSRF checks, and a fresh live-account check for each approval read or decision. The guarded `/usage/pairing` page and `/api/usage/pairing` routes connect these methods to the server transport; live qualification remains separate. The CLI records the browser-approved account and sends its explicit terminal confirmation before enrollment. Neither browser approval nor terminal confirmation creates a namespace or grants upload access.

## Terminal enrollment adapter

The [terminal enrollment wire](usage-terminal-enrollment.md) connects six canonical terminal operations to the existing pairing and account methods. Separate client and server codecs validate retained terminal observations and authoritative RPC results. The HTTP factory derives account routing from a checked reservation, refuses browser Origin before dispatch and retains success expiry through final response delivery. The production router includes this path behind the worker and enrollment fences; both remain closed by default. The macOS CLI driver connects native credential custody and HTTPS enrollment. Its live qualification and operational activation remain separate acceptance steps.

## Internal account enrollment

`reserveEnrollment` creates one immutable reservation only for a live, terminal-confirmed intent in its canonical Pairing object. It binds the confirmed account, both secret commitments, a random reservation ID, the current recovery generation and the earliest intent/authentication deadline. Exact live retries return the original reservation. `readEnrollmentReservation` verifies both preimages and returns an existing reservation after expiry without renewing it. Pairing storage uses a fully validated, additive schema-v1-to-v2 migration; unknown or corrupt state remains untouched. Older v1-only code refuses the migrated row. Preserve that refusal during rollback; never reset or strip enrollment state to reopen an older implementation.

`AccountEnrollment` resolves this reservation directly through the real Pairing binding. Its enrollment, namespace and self-revocation operations accept only the three-field proof, never a caller-provided account or grant. The canonical account-object name is stable across recovery generations. RPC replies are copied into exact data schemas; only the runtime-added disposal symbol is handled separately at this internal transport boundary. The `enroll` response and its receipt readback never return the namespace. Routine admission has the separate two-field contract below.

The first enrollment durably records a pending random namespace, then conditionally writes and verifies its immutable 160-byte `CONTROL` anchor, then atomically records the device and receipt in the account object. The anchor contains only fixed-width account, namespace, genesis intent/reservation, generation and timestamp fields. Reads validate its exact bytes, metadata and SHA-256 checksum under bounded time and size limits. R2 paths and metadata do not include the namespace key or secret preimages. This avoids a fleet-wide namespace derivation secret and preserves the same namespace across subsequent devices. There is no second authorization commit in Pairing.

An exact committed receipt can be read back after expiry. A revoked device stays revoked on retry. `namespaceForEnrollment` requires both original preimages, an unexpired reservation, a matching anchor and a non-revoked device checked again at the final account transaction. An upload secret alone cannot recover the namespace. `revokeEnrollment` can revoke only the proof's own already-enrolled device; it cannot target another device or create one. Receipt readback and self-revocation do not depend on anchor availability because neither discloses the namespace nor grants new authority. All persisted device identities are checked against their reservation. A genesis-completion record identifies the retained device that completed the original enrollment or explicit fresh recovery. The lifetime cap is 128 device receipts, including revoked entries; the implementation never evicts identity to reclaim capacity.

Account rows retain the unchanged enrollment table SQL and anchor bytes. Valid v1 pending state gains null completion; valid active state derives original completion from its already-retained genesis device. The constructor validates the complete v2 state and canonical object binding, then atomically adds six admission tables, including the immutable revision journal, and advances to schema 3. It preserves enrollment payload bytes for v2, revision, timestamps, namespace, receipts and revocation. Progress starts at zero for every retained device, including revoked devices. Unknown, corrupt or partially upgraded state remains untouched. Older binaries refuse the additional tables/version; rollback must not drop them or strip completion to reopen an older implementation.

### Closed recovery states

A lost first-write response can be reconciled by the same still-live reservation using the retained pending namespace and exact R2 readback. Ordinary `enroll` with a different reservation still returns `recovery_required`; an expired original proof returns `expired`.

The separate internal `recoverPendingEnrollment` operation accepts a fresh, same-account reservation created no earlier than the retained anchor. It requires retained valid pending state and preserves the exact original namespace and anchor, conditionally writing those retained bytes if the anchor write never completed. Its final account transaction enrolls only the fresh credential and records a `fresh-recovery` completion. It never fabricates a receipt for the expired original grant. If recovery wins while the original call awaits storage, that original call cannot activate its credential. A competing recovery or an already-active account remains closed to replacement.

An exact committed recovery can return its existing non-secret receipt after expiry or restart; revocation remains effective. Namespace retrieval still requires a live, non-revoked reservation through the separate method. This internal recovery path has local runtime tests but no public transport, CLI integration or live-provider qualification. It cannot recover a missing account object or replace general disaster recovery.

An empty account object with an existing anchor, or an active object with a missing or mismatched anchor, cannot create a new namespace. Missing, malformed or changed `USAGE_ENROLLMENT_GENERATION` closes authority rather than automatically migrating state. The generation lives outside the restored object and must change under a globally closed restore procedure. Neither this fence nor two stores detect arbitrary administrative rollback within an unchanged generation, or simultaneous deletion of both stores. Local tests are not evidence that those operational recovery requirements have been met. The external `RestoreFence` implements close, drain and epoch publication. Qualify the closed restore and recovery procedures before relying on them for live enrollment; source implementation alone does not prove disaster recovery.

## Numeric staging

The staging adapter accepts only the v1 binary media type `application/vnd.aicharts.usage-v1`. It bounds the stream to 884,760 bytes, validates the existing wire policy and records, and re-encodes an owned canonical frame before writing. It never persists the raw request, caller headers, filenames, URLs or arbitrary metadata. The authenticated account's routing scope is separate from `Usage.accountId`, which identifies a provider account or subscription observation.

Objects use account-scoped, content-addressed keys. Conditional creation prevents overwrites. Repeated or uncertain writes require bounded readback of the exact canonical bytes and derived metadata. Fixed error codes do not reflect request data. A receipt explicitly says `staged` and `accepted: false`; staged objects cannot feed rankings, billing, private analytics or an acknowledgment that deletes a local outbox entry.

The fixed numeric protocol excludes free-form chat fields, unknown fields and trailing data. It cannot prove honest measurement or prevent a malicious client from encoding information in numeric counters or fixed-width identifiers. Encryption protects transport or storage confidentiality; it does not establish those stronger privacy or anti-gaming claims.

The separate [admission batch contract](usage-admission-v1.md) wraps up to 256 exact single-Usage frames or tombstone operations with fixed sequence/predecessor fields. Its internal R2 helpers conditionally store one bounded batch object and one exact terminal-journal object, with full byte/checksum/metadata readback. Journal keys are revision-addressed to reject forks. These helpers require authenticated durable caller custody; they do not authenticate, decide acceptance, publish subject heads or settle a local ledger. A journal's structural validity is not receipt provenance. These helpers do not change staging semantics; the admission HTTP handler is composed separately below.

## Account admission

`AccountEnrollment.admitBatch({ uploadSecret, batch })` copies the exact two-field DTO and canonical binary batch before its first await. It verifies the enrolled device's original upload commitment, canonical Hraness account object, retained/current recovery generation and matching namespace anchor. The expired enrollment deadline and polling secret are not routine upload authority. Secret preimages never enter SQL, R2 keys, receipts or error messages.

`AccountEnrollment.readEnrollmentStatus(proof)` is a private, trusted-coordinator read of the durable account control. It requires the proof's own enrolled receipt and returns only the account/generation, state and admission revisions/timestamps, head/live counts, quarantine state and all device receipt states. It never returns the namespace key, polling secret, upload secret or measurement bytes. A revoked proof may read its retained revoked receipt for reconciliation; it cannot reactivate the device.

One account-wide flight reserves a contiguous device sequence range before any batch-object write. After exact batch readback, a synchronous transaction checks revocation and each predecessor, then freezes an irreversible terminal journal. Only exact journal-object readback permits the final atomic head/day-count/sequence publication. Previously published heads remain unchanged while either write is uncertain. Corrections can decrease totals or move days; identical live frames retain their original head, and tombstones never resurrect. Any conflicting member rejects the entire batch while consuming its sequence range.

Revocation before the frozen decision produces uniform outcome 7; revocation afterward cannot alter that decision. Exact pending/latest retries remain reconcilable after revocation, including while another flight exists. An active authenticated device may help one retained predecessor before reserving its own batch, but never receives another device's receipt. No account-wide asynchronous mutex spans R2 calls. Late responses are checked against the retained exact flight, so they cannot replace or clear a successor. Object conflicts quarantine admission without discarding evidence; uncertain I/O retains the flight.

Policy version 1 is source-owned and immutable: registry revision 1, no known models, UTC days 0 through 100,000,000 and safe timestamps through 8,640,000,000,000,000. Puts must match the existing imported-token profile: one Usage, unknown model/provider-account/authentication, no prompt/interval records, imported evidence, and Codex, Claude Code or Devin. A new request must not contain a measurement later than fresh actual server time, even if it would deduplicate or conflict. Historical imports are allowed. Retained retries use the fixed policy and frozen time, not a sliding decode window. This does not attest the client's clock or usage.

Admission and enrollment share a durable observation floor; clock regression closes the operation rather than clamping time. Startup validates the exact six-table manifest and all bounded retained relationships, including sequence ownership, nondecreasing journal commit times, journal order, head provenance against still-retained receipts and day counters. Frozen pending decisions are reconstructed against unpublished predecessor heads, never against current device eligibility. Local consistency checks cannot authenticate pruned journal history or detect an otherwise-valid administrative rollback.

The v1 admission limits are 128 lifetime device identities, 100,000 retained subjects including tombstones, 65,536 live Usage heads per UTC day, one flight and 4,096 terminal journal revisions including rejections. Capacity is checked before reservation; net day changes permit corrections and tombstones without transient over-counting. Full capacity still permits exact replay and self-revocation. At most 8,192 immutable admission objects carry 613,449,728 payload bytes per account/generation; the namespace anchor is additional. SQL binary payload is bounded to 51,320,072 bytes, excluding metadata, indexes and physical SQLite allocation. These are implementation ceilings, not purchased capacity, operational cost qualification or an unlimited-history promise.

The private daily projection below uses only unique current live heads grouped by UTC day, never rejected requests, superseded history or tombstones. The 65,536 daily limit matches the current rollup input ceiling; importing 100,000 live occurrences on one day is intentionally unavailable until a separately reviewed expansion. No public query or leaderboard is activated by admission source.

## Private daily query

`AccountEnrollment.readImportedDays` accepts a strictly validated account/session assertion and a contiguous range of at most 31 UTC days. It is a trusted-coordinator RPC: its DTO establishes syntax, not authentication. The HTTP adapter below verifies the production workload before body processing or account-object selection and derives the account and expiry from the application's live server session. Browser callers must never receive native device upload credentials.

The response reports current imported Codex, Claude Code and Devin occurrence counts, accounted tokens and output tokens by day. Token sums use exact integers serialized as canonical decimal strings; reasoning tokens already included in output are not added again. The fixed `imported-tokens-v1` profile and `partial` coverage do not imply complete history, known models, prices, subscriptions, prompts, intervals or turn measurements. Empty days remain explicit. The journal revision and commit time identify the snapshot; rejected or duplicate submissions can advance that revision without changing totals.

The query streams at most 100,000 retained heads into at most 93 provider/day cells and validates selected heads against the retained day counts. Its owned response is bounded to 16 KiB. Only published live heads contribute: corrections can decrease totals or move a measurement between days/providers, while tombstones and uncertain pending admission do not contribute. The query checks canonical account binding, active enrollment, current recovery generation, quarantine, durable clock floors and session expiry before and after reading. Exact namespace-anchor readback precedes the final synchronous snapshot. Revoked devices do not erase previously accepted account records or prevent the account owner from reading them.

Query transactions preserve already initialized SQL and R2 state, including enrollment and admission clocks. First access to an absent object still performs the existing constructor's schema and initial-row setup; existing constructor migrations are unchanged. This read path does not qualify an administrative restore or missing-object recovery.

`bun run scripts/usage-worker-tools.ts test-private-days` runs the focused local Worker suite. It covers exact large totals, corrected and deleted records, pending publication, credential/account boundaries, expiry, clock regression, generation/anchor changes, corruption, restart and initialized-state preservation. The contract tests also reject extra fields, accessors, sparse arrays and noncanonical integers. The guarded application query and dashboard are implemented separately; these local tests do not establish their production activation.

## Private query HTTP

`createPrivateDaysCoordinator` composes the existing production-workload verifier with fixed `POST https://usage.aicharts.io/internal/usage/days`. Workload verification precedes body consumption and canonical account-object selection. Requests require JSON media/accept headers, no cookies or content encoding, and at most 256 canonical ASCII bytes. Domain success or a bounded domain refusal travels in a correlated HTTP 200 envelope; malformed framing, workload refusal and operational failure use fixed 400, 401 or 503 responses. All responses are private and non-cacheable.

`createVercelPrivateDaysTransport` captures the current public Vercel request-context token and composes the live `beginUsagePrivateReadSession` scope. Its default availability and session checks require `AICHARTS_USAGE_PRIVATE_READ_ENABLED=1` plus the separate browser-auth production configuration. Product input contains only the date range. The transport derives account/expiry from one live Accounts read, rechecks both fences and authority across awaits, and never forwards browser cookies or OAuth bearers to the Worker. The Worker independently repeats session expiry and workload checks before and after its RPC. Constructing either factory performs no request. The [guarded application route and dashboard](usage-identity.md#private-daily-reads) now exist in source; missing required flags keep the production router at the fixed private `503`. The activation runbook records observed production configuration separately from source defaults and live query qualification.

The client and Worker register their existing work promises before I/O, allow eight unsettled requests per instance, and apply 15-second client, 10-second Worker and five-second stage deadlines. Capacity remains held until underlying work and cleanup settle; timeouts do not promise cancellation of dispatched RPC work. RPC replies are disposed after ownership and correlation checks. The response cap is 16 KiB. Exact ASCII size counting and a complete owned JSON projection prevent inherited `Object` or `Array` serialization hooks from changing or inspecting the emitted record. Consumers receive ordinary frozen DTO arrays.

`bun run scripts/usage-worker-tools.ts test-private-days-http` verifies the actual local RPC boundary. Focused Bun tests cover the codec, session fences, framing, timeout/disposal behavior and isolated public Vercel helper binding. These synthetic tests do not qualify live identity, platform disconnect behavior, fleet-wide abuse controls, paid capacity or production recovery. The source defaults leave the Worker disabled; actual deployment configuration and qualification are recorded separately.

## Admission HTTP

`createAdmissionHttpHandler` composes the existing account admission method with the fixed `POST https://usage.aicharts.io/v1/batches` route. The production router includes it only behind the worker and admission fences, both closed by default. Requests require `application/vnd.aicharts.usage-batch-v1`, an exact `Accept: application/vnd.aicharts.usage-journal-v1` header and the enrolled device's upload bearer. Cookies and content encoding are refused. A canonical optional content length must match the bounded stream; batches cannot exceed 82,024 bytes. Framing and canonical batch validation precede account selection. The account object verifies the device credential, account, generation and namespace authority.

Only an owned terminal journal correlated with every operation in the submitted batch can produce HTTP 200. A terminal rejection journal also uses 200 so the sender can settle its consumed sequence range correctly; 200 alone is not acceptance of every measurement. The adapter returns fixed 400, 401, 409 or 503 failures for invalid framing, missing or incorrect device authority, blocked uploads and operational failure. Failure bodies contain no request data or credential. Responses are private and non-cacheable.

The adapter registers request lifetime before body acquisition, admits at most eight unsettled operations per isolate, and applies a 15-second overall deadline and a 10-second RPC deadline. Timeout does not cancel a committed durable decision. Capacity remains held until actual stream cancellation or RPC settlement and reply disposal finish. Retrying the same frozen batch can recover its exact terminal journal after a lost reply or object restart. These limits do not qualify fleet-wide abuse controls or paid capacity.

`bun run scripts/usage-worker-tools.ts test-admission-http` runs the focused local Worker contract. It covers actual committed RPC reply loss and restart replay, concurrent retries, expired pairing grants, revocation, cross-account/device denial, maximum frames, malformed bodies, delayed clocks and reply disposal. The full Worker gate also includes this suite. Synthetic credentials and local R2/DO fixtures establish these tested behaviors; the native upload transport is implemented, while production binding and live upload qualification remain separate activation checks.

## Leaderboard consent

`AccountEnrollment` schema 5 adds a `leaderboard` record to account state: a consent boolean, the latest grant commit time, a bounded public handle and a decision timestamp that orders replays. The additive migration starts every existing account unpublished; no migration can opt an account in. `readLeaderboardConsent` answers the session-scoped status view, and `setLeaderboardConsent` commits the decision under the same restore-fence lease as other account mutations. An account alarm is armed before commit and retries delivery of the latest saved choice after a failed index apply, including the initial grant and withdrawals. Exact decision retries remain idempotent; a distinct decision in the same clock millisecond fails closed instead of silently losing a withdrawal. Consent reads and withdrawn projections remain observable even when the numeric admission journal is quarantined.

The index refuses a public handle already held by another member and a new publisher beyond its 128-member capacity. The account conditionally restores its prior decision only after either explicit refusal and only while the candidate remains current; a concurrent newer choice is never undone. The public route returns `handle_unavailable` or `publishing_full` with HTTP 409. An uncertain write returns unavailable and requires status reconciliation, not an assertion that the old consent still applies.

`readLeaderboardProjection` is the internal index-facing read only. Its reply carries `accountId` solely so the index can match the member it queried; the field never enters public output. The projection reuses the private-days read path, so corrections can decrease totals and tombstoned or superseded data never contributes. A `consent:false` reply is the index's authoritative removal signal for a stale member.

`createConsentHttpHandler` composes these methods with the fixed `POST https://usage.aicharts.io/internal/usage/consent` route. It mirrors the private-days adapter: workload verification precedes body consumption and canonical account selection, the request body is at most 256 canonical ASCII bytes carrying only the consent operation, and domain outcomes travel in a correlated HTTP 200 envelope. The production router fences it with the private-read qualification (`AICHARTS_USAGE_AUTH_ENABLED` plus `AICHARTS_USAGE_PRIVATE_READ_ENABLED`), independently of public-read activation. `bun run scripts/usage-worker-tools.ts test-consent-http` runs the focused local suite.

## Public leaderboard index

`LeaderboardIndex` (`PUBLIC_INDEX` binding) is the single materialized index object named `leaderboard-index-v1:public`. Consenting members change only through fenced `applyConsent` calls from the owning account object; stale replays at or before the newest applied decision are no-ops, and a withdrawal records a bounded tombstone so an older grant replay cannot resurrect the member. At most 128 members and 256 tombstones are retained.

Public reads serve the stored snapshot without account RPC or storage mutation. A grant verifies its own account; scheduled alarms verify at most eight stale members per cycle. The index continuously re-arms while members exist, begins refreshing after five hours and 44 minutes, and drains backlog or retries unavailable sources at one-minute intervals. This leaves 16 batches before the six-hour freshness deadline at maximum capacity. Persisted attempt order prevents one unavailable account from starving later accounts. Public reads omit rows whose affirmative source verification is older than six hours. The snapshot contains handles, exact decimal observed-token totals, record counts, per-row coverage windows and verification times; it contains no email, account ID, device ID or session data. A verified withdrawal, enrollment loss or malformed authority reply removes the member. New conflicting handle claims cannot evict the incumbent.

An index created by an older build may contain members without a scheduled alarm. Before enabling public reads after upgrade, prove the index is empty or qualify controlled re-publication that arms and refreshes its existing members. A deployment alone does not establish that historical state has been refreshed; public reads must remain mutation-free.

`createLeaderboardHttpHandler` serves the fixed unauthenticated `GET https://usage.aicharts.io/v1/leaderboard` route. Because the only body it can emit is the materialized snapshot, it verifies no identity and accepts none: requests carrying cookies, authorization, a body or non-exact framing are refused. Successful snapshots use `public, max-age=60`; errors use `private, no-store` so an unavailable response does not delay recovery. Cached snapshots can retain withdrawn entries for up to one minute. The production router gates it with its own `AICHARTS_USAGE_PUBLIC_READ_ENABLED=1` fence; private collection flags do not open it, and the public flag never opens consent writes or private reads. `bun run scripts/usage-worker-tools.ts test-leaderboard` and `test-leaderboard-http` run the focused local suites.

## Validation and activation

Run `bun run usage:worker:check` after installing frozen dependencies. This generates ignored runtime types from the pinned Wrangler configuration, typechecks the Worker independently of Next.js, then runs `*.worker.ts` in Cloudflare's [local Vitest runtime](https://developers.cloudflare.com/workers/testing/vitest-integration/write-your-first-test/). `bun run check` includes this gate for local integration and both existing CI callers. The test runner has no publish mode and directs its configuration/logs to the ignored local fixture directory. It refuses execution when a legacy user `.wrangler` path exists, because pinned Wrangler prefers that path over XDG configuration. The runner checks existence only and never reads, moves or deletes that personal configuration. These guards do not constitute OS-enforced process isolation.

Local restart helpers preserve Durable Object storage across instance teardown. They test persistence, retry and refusal behavior, not production disaster recovery. Actual SQLite [point-in-time recovery](https://developers.cloudflare.com/durable-objects/api/sqlite-storage-api/#pitr-point-in-time-recovery-api) is unavailable locally and does not restore R2 or other objects. Do not treat R2 object version identifiers as recoverable bucket version history.

The production entrypoint composes its HTTP factories once per isolate and routes only the exact enrollment, pairing, admission, private-days, consent, leaderboard and detailed-stats URLs. Each route requires `AICHARTS_USAGE_WORKER_ENABLED=1` plus its own operation fence (`AICHARTS_USAGE_ENROLLMENT_ENABLED`, `AICHARTS_USAGE_PAIRING_ENABLED` with browser auth, `AICHARTS_USAGE_ADMISSION_ENABLED`, `AICHARTS_USAGE_PRIVATE_READ_ENABLED` with browser auth, or `AICHARTS_USAGE_PUBLIC_READ_ENABLED` for the anonymous public read). Detailed stats additionally requires `AICHARTS_USAGE_STATS_ENABLED=1`: reads retain authentication and private-read controls, while upload, status and abandonment retain admission controls. Its snapshot contract and limits are documented in [the activation runbook](usage-activation.md#detailed-snapshot-profile). Missing or non-string-`1` fences, a zero generation, or malformed Durable Object/R2 bindings return the fixed private `503` before handler dispatch. The activation runbook records which flags have been observed in production; this source contract does not imply every feature is active or qualified.

Before activation, complete and independently review:

- Qualify the implemented coordinator transports, callback dispatch and approval UI/routes with real fresh authentication and approval. The current handoff uses an intent-bearing browser URL; a human-code flow and pre-creation abuse limits require their own acceptance evidence. Keep the implemented cookie, CSRF and live-account checks intact.
- Qualify the implemented CLI enrollment, account confirmation and private credential custody. Namespace recovery and explicit local-ledger reindex need their own reviewed evidence; the CLI includes `reindex-plan` and `reindex-prepare`, while general recovery remains limited. Retain original ledger keys and pending history.
- Qualify the implemented authenticated upload transport and source-to-native-to-server path, measure admission capacity and cost, and verify accepted-head queries and materialization. Source and synthetic tests do not qualify these operational paths. An R2 write alone never commits visibility.
- Qualify the implemented external recovery fence, immutable receipts and tombstones, bounded reconciliation and credential invalidation after restore. Restored object state must not independently authorize reopening.
- Exact owner/resource/cost qualification, private bindings, workload authority, recovery procedures and a bounded live upload/revocation/readback test. Keep public activation disabled until this evidence exists.

The remaining product work and delivery evidence belong in [the implementation plan](../kb/plans/usage-leaderboard.md). [Usage identity](usage-identity.md) owns the browser registration and activation fence; [the wire contract](usage-wire-v1.md) owns measurement bytes.
